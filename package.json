{
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB=toallah_db",
    "deploy": "wrangler pages deploy public --project-name toallah",
    "db:init": "echo db:init is disabled until ToAllah migrations are reviewed"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}