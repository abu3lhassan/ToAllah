# First Login Profile Completion — تقرير التغييرات

## 1. ملخص التغيير

عند دخول القارئ من بوابة القراء، يتم فحص `phone` و `country` في `readerProfile`.
إذا كان أحدهما فارغاً، يُمنع القارئ من رؤية البوابة الطبيعية ويُعرض له نموذج إكمال إلزامي.
بعد الحفظ تُعاد تهيئة البوابة الطبيعية تلقائياً.

---

## 2. الملفات المعدلة

| الملف | نوع التعديل |
|---|---|
| `functions/api/[[path]].js` | تعديل `readerPortal` + إضافة `updateReaderProfile` + إضافة route |
| `public/app.js` | إضافة `COUNTRY_CODES` + تعديل `doLookup` داخل `setupReaderLogin` |

---

## 3. الدوال المعدلة

### Backend (`functions/api/[[path]].js`)

| الدالة | التغيير |
|---|---|
| `readerPortal` | تعديل SELECT ليشمل `id` و `phone`؛ إضافة `id` و `phone` في `readerProfile` |
| `updateReaderProfile` | **جديدة** — تُضاف بعد `readerPortal` مباشرة |

### Frontend (`public/app.js`)

| الدالة/الثابت | التغيير |
|---|---|
| `COUNTRY_CODES` | **ثابت جديد** في أعلى الملف (سطر 5) |
| `doLookup` (داخل `setupReaderLogin`) | إضافة شاشة الإكمال قبل العرض الطبيعي |

---

## 4. الـ Endpoint الجديد

```
PATCH /api/reader/me/profile
```

**Body:**
```json
{
  "identity": "access_code_or_serial_or_phone_or_name",
  "phone": "+966501234567",
  "country": "السعودية"
}
```

**Response عند النجاح:**
```json
{ "ok": true }
```

**Errors:**
- `400` — الهوية/الدولة/phone فارغ
- `400` — phone لا يطابق `^\+\d{8,15}$`
- `404` — لم يُعثر على القارئ

---

## 5. الحقول المسموح بتعديلها

| الحقل | في `updateReaderProfile` |
|---|---|
| `phone` | ✅ |
| `country` | ✅ |
| `updated_at` | ✅ (تلقائي `datetime('now')`) |

---

## 6. الحقول الممنوع تعديلها

لا يُعدَّل أي حقل غير المذكورين أعلاه. محظور صراحة:

`reader_name` · `access_code` · `serial_code` · `start_juz` · `parts_count`  
`group_id` · `created_by_user_id` · `status` · `notes` · `id` · `created_at`

---

## 7. طريقة بناء رقم الهاتف

1. المستخدم يختار الدولة من القائمة الثابتة (`COUNTRY_CODES`).
2. إذا اختار دولة معروفة، يُستخدم `code` المرتبط بها تلقائياً.
3. إذا اختار "أخرى"، يظهر حقل لإدخال مفتاح الدولة يدوياً (يبدأ بـ `+`).
4. رقم الجوال المُدخل يُجرَّد من غير الأرقام.
5. إذا بدأ الرقم المحلي بـ `0`، يُحذف الصفر.
6. الرقم النهائي = `countryCode + localPhoneWithoutLeadingZero`.

**مثال:**
```
الدولة: السعودية  →  code: +966
الرقم المُدخل: 0501234567  →  بعد المعالجة: 501234567
الرقم المُخزن: +966501234567
```

**نمط التحقق النهائي:** `/^\+\d{8,15}$/`

---

## 8. طريقة الاختبار

### حالة 1 — قارئ phone/country فارغان
- ادخل من بوابة القراء بـ identity قارئ لا يملك phone/country.
- **متوقع:** تظهر شاشة إكمال بيانات بدلاً من البوابة الطبيعية.

### حالة 2 — القارئ يُكمل بياناته
- اختر دولة، أدخل رقم الجوال، اضغط "حفظ وتأكيد".
- **متوقع:** `PATCH /api/reader/me/profile` يُستدعى → عند النجاح تُعرض البوابة الطبيعية.

### حالة 3 — قارئ بياناته مكتملة
- ادخل بـ identity قارئ لديه phone وcountry.
- **متوقع:** البوابة الطبيعية تظهر مباشرة.

### حالة 4 — قارئ بدون readerProfile
- قارئ أُضيف في ختمة يدوياً بدون profile مرتبط.
- **متوقع:** `readerProfile === null` → السلوك القديم يبقى كما هو (لا شاشة إكمال).

### تحقق Backend (phone validation):
```
+ فارغ           → 400
+ بدون +         → 400
+ مع حروف        → 400
+ أقل من 8 أرقام → 400
+ أكثر من 15 رقم → 400
+ +966501234567  → 200 ok
```

---

## 9. المخاطر

| المخاطرة | التقييم | الحل |
|---|---|---|
| قارئ يُدخل رقم جوال لشخص آخر | منخفض — الـ identity هو حارس الدخول | لا fix مطلوب |
| القارئ يضغط تحديث الصفحة أثناء الإكمال | لا خطر — يُعرض النموذج مجدداً | — |
| قارئ يستخدم نفس الـ identity في جهازين في نفس الوقت | لا تعارض — كل PATCH مستقل | — |
| تغيير `phone` قد يكسر البحث المستقبلي بـ phone | الـ identity المحفوظ في localStorage هو access_code/serial/name في الغالب | تنبيه: لا تبدّل identity المحفوظ |

---

## 10. هل تم لمس DB/Schema؟

**لا.** الحقلان `phone` و `country` موجودان في `managed_reader_profiles` مسبقاً.
لم يُنشأ أي migration ولم يتغير أي schema.

---

## 11. هل تم commit/push/deploy؟

**لا.**
